# ── Environment / secrets ─────────────────────────────────────────────────────
.env
*.env
.env.local

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
dist/
build/

# ── Logs / tmp ────────────────────────────────────────────────────────────────
*.log
*.bak
*.bak.*
*.BACKUP

# ── Invoice PDFs (generated at runtime) ───────────────────────────────────────
api-key-manager/invoices/

# ── Plugin zips (large binaries — distribute separately) ──────────────────────
api-key-manager/static/plugin_zips/

# ── Tutorial video build artifacts (large, regenerable from tutorial_studio scripts) ──
api-key-manager/tutorial_studio/_workspace/
api-key-manager/static/portal/tutorial/videos/

# ── User-uploaded content (PII — ID docs, qualification proofs, listing photos) ──
api-key-manager/static/uploads/
api-key-manager/static/estate_images/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Secrets (added 2026-08-16) ─────────────────────────────────────────────────
.pg_password

# ── DB dumps / server backups (added 2026-08-16) ──────────────────────────────
ai-backend-backup-20260526/
mysql_backup_20260526.sql
phixtra-data-sync-backup-20260526/

# ── More user-uploaded content (PII) ───────────────────────────────────────────
api-key-manager/static/portal/datasource_uploads/
api-key-manager/static/portal/product_images/

# ── Local runtime DB, not source ───────────────────────────────────────────────
rd-log/rdlog.db

# ── Separate standalone project with its own git history — belongs in its own repo ──
stocklist-tool/
